export const settlementDaysBySource: Record<string, number> = {
  // D+N values por origem de marketplace
  'Mercado Livre': 15,
  Shopee: 14,
  Shopify: 15,
  Gateway: 15,
  Outro: 15,
  default: 15,
};
